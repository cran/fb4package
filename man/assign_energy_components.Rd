% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/15.0-analysis-core.R
\name{assign_energy_components}
\alias{assign_energy_components}
\title{Fill energy budget components from a named source list}
\usage{
assign_energy_components(budget, src, mapping, idx = NULL)
}
\arguments{
\item{budget}{Named list with one sub-list per energy component, each
containing at least \code{estimate} and \code{se} slots.}

\item{src}{Named list (or data frame column) that holds the raw
\code{<prefix>_est} / \code{<prefix>_se} values.}

\item{mapping}{Named character vector mapping component names (keys) to
source field prefixes (values), e.g.
\code{c(consumption_energy = "mean_total_consumption", ...)}.}

\item{idx}{Integer index used for vector extraction (individual branch).
\code{NULL} for scalar extraction (population / TMB branches).}
}
\value{
The updated \code{budget} list.
}
\description{
Internal helper that populates the \code{estimate} and \code{se} slots of
several energy components inside a budget result list.  Avoids repeating the
same assignment pattern across population, individual, and TMB branches of
\code{get_energy_budget_uncertainty()}.
}
\keyword{internal}
