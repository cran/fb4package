% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/10-data-processing.R
\name{prepare_simulation_data}
\alias{prepare_simulation_data}
\title{Prepare all simulation data}
\usage{
prepare_simulation_data(
  bio_obj,
  strategy,
  fit_to = NULL,
  fit_value = NULL,
  first_day = 1,
  last_day = NULL,
  validate_inputs = TRUE,
  oxycal = 13560,
  output_format = "simulation",
  observed_weights = NULL,
  covariates = NULL
)
}
\arguments{
\item{bio_obj}{Bioenergetic object (must be pre-validated)}

\item{strategy}{Strategy to use: "binary_search", "optim", "bootstrap", "mle", "hierarchical"}

\item{fit_to}{Target type for fitting (e.g., "Weight"); optional for direct strategy}

\item{fit_value}{Target value to fit to; optional for direct strategy}

\item{first_day}{First simulation day}

\item{last_day}{Last simulation day}

\item{validate_inputs}{Whether to perform comprehensive validation, default TRUE}

\item{oxycal}{Oxycalorific coefficient (J/g O2), default 13560}

\item{output_format}{Output format: "simulation", "tmb_basic", "tmb_hierarchical"}

\item{observed_weights}{Data frame with columns: individual_id, initial_weight and observed_weight}

\item{covariates}{Optional covariate matrix or data frame or choose a column of individual_data}
}
\value{
For \code{output_format = "simulation"} (default), a named list
  with seven elements: \code{species_params} (processed species parameter
  sub-lists), \code{temporal_data} (processed temporal arrays),
  \code{simulation_settings} (processed settings), \code{metadata}
  (processing timestamp, duration, prey species, data sources),
  \code{n_days} (integer), \code{temperatures} (numeric vector), and
  \code{initial_weight} (numeric scalar). For
  \code{output_format = "tmb_basic"} or \code{"tmb_hierarchical"},
  returns a list formatted for TMB model fitting (structure differs).
}
\description{
Master function that processes and validates ALL data required for FB4 simulation.
Combines species parameter processing with temporal data processing.
}
\examples{
\donttest{
# Requires a fully-configured Bioenergetic object; see ?Bioenergetic
# bio <- Bioenergetic(...)
# sim_data <- prepare_simulation_data(bio, strategy = "direct")
}
}
